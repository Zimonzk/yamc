#version 330 core

layout(location=1) in vec2 UV;

out vec4 color;

uniform sampler2D my_sampler;
uniform vec4 textcolor;

void main(){
  color = vec4(1, 1, 1, texture(my_sampler, UV).a) * textcolor;
}
